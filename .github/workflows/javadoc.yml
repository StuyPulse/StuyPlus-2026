name: "Javadoc Generator"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
on:
  push:
    branches:
      - main
  workflow_dispatch:
permissions:
  contents: write
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 1.17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: 'temurin'
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Build with Gradle
        run: ./gradlew build
      - name: Generate docs
        run: ./gradlew javadoc
      - name: Add favicon and logo
        run: |
          cp assets/javadoc/favicon.ico build/docs/javadoc/
          cp assets/logos/StuyPlusLogo.png build/docs/javadoc/
      - name: Move generated docs to docs/ directory
        run: |
          cp -r build/docs/javadoc/. docs/
      - name: Clean up build artifacts
        run: rm -rf build/ .gradle/
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin docs
          git checkout docs
          git add docs/
          git commit -m "DOCS: Automated documentation update from main" || echo "No changes to commit"
          git push origin main
